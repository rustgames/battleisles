# Loaded by direnv when entering this directory. Contains no secrets.
# Local-only overrides go in .env.local (gitignored).
dotenv_if_exists .env.local
# Example for a future secret from Key Vault (helpers: docs/direnv/direnvrc):
# has kv_export && kv_export SOME_API_KEY battleisles-some-api-key
